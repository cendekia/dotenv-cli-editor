var assert = require('assert');
const expect = require('chai').expect;


describe('dotenv-cli edit', () => {
    it('this test should be replaced with real tests', () => {
        expect(true);
    })
})